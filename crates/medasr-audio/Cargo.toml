[package]
name = "medasr-audio"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Cross-platform audio capture (cpal), resampling (rubato), VAD."

[lints]
workspace = true

[dependencies]
medasr-types         = { workspace = true }
medasr-secure-buffer = { workspace = true }
thiserror            = { workspace = true }
tracing              = { workspace = true }

cpal     = "0.15"
rubato   = "0.15"
rtrb     = "0.3"
rustfft  = "6"

[dev-dependencies]
hound = "3"  # WAV reading for tests

[features]
# `integration` enables tests that require a real microphone.
integration = []
