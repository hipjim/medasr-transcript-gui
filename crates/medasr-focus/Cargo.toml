[package]
name = "medasr-focus"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Two-factor focus pin: per-OS active-window probes."

[lints]
workspace = true

[dependencies]
medasr-types = { workspace = true }
thiserror    = { workspace = true }
tracing      = { workspace = true }

# Most accurate cross-platform "what is the focused window?" lookup.
# Works on macOS, Windows, X11; refuses to run on Wayland (matches plan).
active-win-pos-rs = "0.9"
